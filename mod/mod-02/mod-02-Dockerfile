FROM golang:latest
MAINTAINER Ruby on Racetracks

RUN go version && cat /etc/os-release && \
    apt-get update && apt-get upgrade -y && \
    apt-get install -y bash git hugo && \
    adduser --disabled-password --gecos '' winner

USER winner

WORKDIR /home/winner/neutrino
